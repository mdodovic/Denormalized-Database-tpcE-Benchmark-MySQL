SELECT CA_ID, HS_CA_ID, CA_BAL, HS_S_SYMB, 
		LT_S_SYMB, HS_QTY, C_ID, CA_C_ID, LT_PRICE 
	FROM dbo.CUSTOMER_ACCOUNT 
		LEFT OUTER JOIN dbo.HOLDING_SUMMARY on HS_CA_ID = CA_ID, 
			dbo.LAST_TRADE, dbo.CUSTOMER
	WHERE LT_S_SYMB = HS_S_SYMB and C_ID = CA_C_ID
